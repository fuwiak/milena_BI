# =============================================================================
# Production requirements для Streamlit-дашборда (Railway / Streamlit Cloud).
# Версии синхронизированы с локальным окружением, в котором был засериализован
# model.pkl и parquet-кэш. Расхождение pandas 2.x vs 3.x ломает StringDtype
# при unpickling — поэтому ВАЖНО держать те же версии, что и в dev-окружении.
# =============================================================================

# Core data
pandas==3.0.2
numpy==2.2.6
pyarrow==19.0.1
scipy==1.17.1

# ML runtime (для .pkl-модели нужны sklearn + lightgbm той же мажорной версии)
scikit-learn==1.8.0
lightgbm==4.6.0
joblib==1.4.2

# BI / Dashboard (версии совместимы с numpy 2.x / pandas 3.x)
streamlit==1.56.0
plotly==6.7.0

# Загрузка raw xlsx (на случай fallback-режима)
openpyxl==3.1.2

# Utilities
PyYAML==6.0.1
python-dotenv==1.0.1
